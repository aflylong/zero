# Icons

This folder holds platform icons generated by Tauri's `tauri icon` CLI from a
single source PNG. The source PNG lives at the repo root: `logo-1024.png`.

To regenerate after replacing the logo:

```
pnpm --filter @guiling/desktop exec tauri icon ../../../logo-1024.png
```
