ignore-workspace-root-check=true
# Don't re-install before every script — install is explicit.
verify-deps-before-run=false
# Don't fail commands just because a transitive postinstall was skipped.
# We pin allowed builds via the root package.json's pnpm.onlyBuiltDependencies.
strict-dep-builds=false
